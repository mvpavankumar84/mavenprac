package Mpackage;

import java.util.HashMap;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class dup {

	public static void main(String[] args) {
		/*String word="tbacdbbact";
		HashMap<Character, Integer> str=new HashMap<>();
		for (int i=word.length()-1; i>=0; i--)
			if (str.containsKey(word.charAt(i))) {
				int count=str.get(word.charAt(i));
				str.put(word.charAt(i), ++count);
				
			} else {
				str.put(word.charAt(i), 1);

			}
		System.out.println(str); */
		
	/*	System.setProperty("webdriver.chrome.driver", "C:\\Browserdrivers\\chromedriver_win32\\chromedriver.exe");
		WebDriver driver = new ChromeDriver();
		driver.get("https://demo.guru99.com/test/newtours/reservation.php");
		List<WebElement> all=driver.findElements(By.tagName("a"));
		System.out.println("list of available links" + " "+ all.size());
		for(int c=0; c<all.size(); c++)
		{
			System.out.println("link name" +c + all.get(c).getText());
		} */
		
		/*HashMap<Integer, String> hash_map = new HashMap<Integer, String>();
		  
        // Mapping string values to int keys
        hash_map.put(10, "Geeks");
        hash_map.put(15, "4");
        hash_map.put(20, "Geeks");
        hash_map.put(25, "Welcomes");
        hash_map.put(30, "You");
  
        // Displaying the HashMap
        System.out.println("Initial Mappings are: " + hash_map);
  */
	}
	

}
